<div class="container">
  <div class="container my-5 header-secondary">
    <h3><%= Date.today.strftime("%B %d, %Y").upcase %></h3>
    <h3><%= Date.today.strftime("%A").upcase %></h3>
    <p class="mt-4">Add your invoices, hours or clients here</p>
  </div>

  <div class="container mb-5 d-flex justify-content-between align-items-center">
    <div class="btn-dashboard-purple">
      <%= link_to 'INVOICE', new_invoice_path, class: 'm-1' %>
    </div>

    <div class="btn-dashboard-purple">
      <%= link_to 'TIME', new_expense_path, class: 'm-1' %>
    </div>

    <div class="btn-dashboard-purple">
      <%= link_to 'RECEIPT', new_expense_path, class: 'm-1' %>
    </div>

    <div class="btn-dashboard-purple">
      <%= link_to 'CLIENTS', clients_path, class: 'm-1' %>
    </div>
  </div>

</div>

