<div class="container">
  <div class="container my-5">
    <%= Date.today.strftime("%B %d, %Y") %>
    <%= Date.today.strftime("%A") %>
  </div>

  <div class="container d-flex justify-content-between align-items-center">
    <%= link_to 'Create Invoice', new_invoice_path, class: 'btn btn-outline-primary' %>

    <%= link_to 'New Time Entry', new_expense_path, class: 'btn btn-outline-secondary' %>

    <%= link_to 'Add Receipt', new_expense_path, class: 'btn btn-outline-secondary' %>

    <%= link_to 'Show All Clients', clients_path, class: 'btn btn-outline-info' %>
  </div>

</div>

